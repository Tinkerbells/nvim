return {
  "echasnovski/mini.surround",
  opts = {
    mappings = {
      add = "<leader>a", -- Add surrounding in Normal and Visual modes
      delete = "<leader>d", -- Delete surrounding
      replace = "<leadek>r", -- Replace surrounding
    },
  },
}
